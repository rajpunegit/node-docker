/// <reference types="node" />
/**
 * Convert an AttestationObject from base64url string to a proper object
 *
 * @param base64AttestationObject Base64URL-encoded Attestation Object
 */
export default function decodeAttestationObject(base64AttestationObject: string): AttestationObject;
export declare enum ATTESTATION_FORMATS {
    FIDO_U2F = "fido-u2f",
    PACKED = "packed",
    ANDROID_SAFETYNET = "android-safetynet",
    ANDROID_KEY = "android-key",
    TPM = "tpm",
    APPLE = "apple",
    NONE = "none"
}
export declare type AttestationObject = {
    fmt: ATTESTATION_FORMATS;
    attStmt: AttestationStatement;
    authData: Buffer;
};
export declare type AttestationStatement = {
    sig?: Buffer;
    x5c?: Buffer[];
    response?: Buffer;
    alg?: number;
    ver?: string;
    certInfo?: Buffer;
    pubArea?: Buffer;
};
